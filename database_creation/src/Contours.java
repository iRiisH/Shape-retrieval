
public class Contours {/*
	public void renderContours()
	{
		
	}
	
	public void renderCanny(Mat img)
	{
		Mat tmp;
		cvtColor(img,tmp,CV_BGR2GRAY);
		Canny(tmp, thr, 10, 100, 3);
	}
	public void renderOccludingContours(3DObject)
	{
		
	}
	public void renderSilhouette(Mat img)
	{
		Mat gray;
		cvtColor(img, gray, CV_BGR2GRAY);
		gray.convertTo(gray, CvType.CV_64FC1);
		Mat mask = Mat.zeros()
		int m = img.cols(), n = img.rows();
		for(int i = 1 ; i < m-1 ; i++)
		{
			for (int j = 1 ; j < n-1 ; j++)
			{
				double pix = gray.get(i, j)
				
			}
		}
	}*/
}
